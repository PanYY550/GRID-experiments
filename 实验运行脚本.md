# 实验运行脚本记录

> **最后更新**: 2026-05-18
> **说明**: 记录每个实验的 SID 训练、SID 推理、TIGER 训练三个阶段的启动方式。

---

## 实验 A: G0 (纯 RQ-VAE, 无 VCF)

**实验描述**: 纯 RQ-VAE 基线，无排斥损失、无对比学习

**SID 训练**:
```bash
# 位于 scripts/run_G0_only_gpu1.sh 内
CUDA_VISIBLE_DEVICES=0 python -m src.train \
    experiment=rqvae_train_flat \
    data_dir=/home/pyy/GRID/src/data/amazon_data/beauty \
    embedding_path=/home/pyy/GRID/embeddings/beauty/pickle/merged_predictions_tensor.pt \
    embedding_dim=768 \
    num_hierarchies=3 \
    codebook_width=256 \
    seed=42 \
    trainer.max_steps=3000 \
    "hydra.run.dir=outputs/G0_only_gpu1/G0_GRID_Baseline/01_sid_train"
```

**SID 推理**:
```bash
# 位于 scripts/run_G0_only_gpu1.sh 内
CUDA_VISIBLE_DEVICES=0 python -m src.inference \
    experiment=rqvae_inference_flat \
    data_dir=/home/pyy/GRID/src/data/amazon_data/beauty \
    embedding_path=/home/pyy/GRID/embeddings/beauty/pickle/merged_predictions_tensor.pt \
    embedding_dim=768 \
    num_hierarchies=3 \
    codebook_width=256 \
    ckpt_path=<ckpt_path> \
    seed=42 \
    "hydra.run.dir=outputs/G0_only_gpu1/G0_GRID_Baseline/02_sid_inference"
```

**TIGER 训练**:
```bash
# 位于 scripts/run_G0_only_gpu1.sh 内
CUDA_VISIBLE_DEVICES=0 python -m src.train \
    experiment=tiger_train_flat \
    data_dir=/home/pyy/GRID/src/data/amazon_data/beauty \
    semantic_id_path=<sid_pt> \
    num_hierarchies=4 \
    seed=42 \
    "hydra.run.dir=outputs/G0_only_gpu1/G0_GRID_Baseline/03_tiger_train"
```

**脚本位置**: `scripts/run_G0_only_gpu1.sh` (一体化脚本，包含全部三个阶段)
**输出目录**: `outputs/G0_only_gpu1/G0_GRID_Baseline/`
**日志目录**: `logs/G0_only_gpu1/`

---

## 实验 B: Q0 (VCF 基线)

**实验描述**: VCF 基线 (λ=0.3/0.2, m=0.5/0.3, CL on, time_decay on)

**SID 训练**:
```bash
# 位于 scripts/run_final_3groups_full_pipeline.sh 内
# 注: use_time_decay, m0, lambda_rep, codebook_entropy_weight 等参数未显式传递，
#     继承自 rqvae_vcf_train_flat.yaml 的默认值。
CUDA_VISIBLE_DEVICES=1 python -m src.train \
    experiment=rqvae_vcf_train_flat \
    data_dir=/home/pyy/GRID/src/data/amazon_data/beauty \
    embedding_path=/home/pyy/GRID/embeddings/beauty/pickle/merged_predictions_tensor.pt \
    embedding_dim=768 \
    num_hierarchies=3 \
    codebook_width=256 \
    data_loading.datamodule.train_dataloader_config.batch_size_per_device=4096 \
    data_loading.datamodule.train_dataloader_config.num_workers=16 \
    data_loading.datamodule.train_dataloader_config.pin_memory=true \
    data_loading.datamodule.train_dataloader_config.persistent_workers=true \
    seed=42 \
    trainer.max_epochs=null \
    trainer.max_steps=3000 \
    model.use_vcr_td=false \
    model.use_vcf=true \
    model.use_tcl=true \
    model.lambda_cl=0.0 \
    model.hamming_radius=2 \
    model.m_full=0.5 \
    model.m_partial=0.3 \
    model.lambda_full=0.3 \
    model.lambda_partial=0.2 \
    model.repulsion_warmup_steps=2000 \
    model.use_cvpm=true \
    model.cvpm_temperature=0.15 \
    model.exposure_counts_path=/home/pyy/GRID/src/data/amazon_data/beauty/exposure_counts.pt \
    model.use_dynamic_margin=false \
    model.alpha=0.0 \
    "hydra.run.dir=outputs/final_3groups/final3g_sid3000_20260428_220333/Q0_GQSS_static/01_sid_train"
```

**SID 推理**:
```bash
CUDA_VISIBLE_DEVICES=1 python -m src.inference \
    experiment=rqvae_vcf_inference_flat \
    data_dir=/home/pyy/GRID/src/data/amazon_data/beauty \
    embedding_path=/home/pyy/GRID/embeddings/beauty/pickle/merged_predictions_tensor.pt \
    embedding_dim=768 \
    num_hierarchies=3 \
    codebook_width=256 \
    ckpt_path=<ckpt_path> \
    seed=42 \
    "hydra.run.dir=outputs/final_3groups/final3g_sid3000_20260428_220333/Q0_GQSS_static/02_sid_inference"
```

**TIGER 训练**:
```bash
CUDA_VISIBLE_DEVICES=1 python -m src.train \
    experiment=tiger_train_flat \
    data_dir=/home/pyy/GRID/src/data/amazon_data/beauty \
    semantic_id_path=<sid_pt> \
    num_hierarchies=4 \
    seed=42 \
    "hydra.run.dir=outputs/final_3groups/final3g_sid3000_20260428_220333/Q0_GQSS_static/03_tiger_train"
```

**脚本位置**: `scripts/run_final_3groups_full_pipeline.sh`
**输出目录**: `outputs/final_3groups/final3g_sid3000_20260428_220333/Q0_GQSS_static/`
**日志目录**: `logs/final_3groups/final3g_sid3000_20260428_220333/`

---

## 实验 C: C2ctrl (VCF λ=0.2/0.1, 对称 HaMR, 无 CL)

**实验描述**: QuaSID 最优 λ，隔离 lambda 变化效果

**SID 训练**:
```bash
# 位于 scripts/run_C2_full_pipeline.sh 内
CUDA_VISIBLE_DEVICES=1 python -m src.train \
    experiment=rqvae_vcf_train_flat \
    data_dir=/home/pyy/GRID/src/data/amazon_data/beauty \
    embedding_path=/home/pyy/GRID/embeddings/beauty/pickle/merged_predictions_tensor.pt \
    embedding_dim=768 \
    num_hierarchies=3 \
    codebook_width=256 \
    data_loading.datamodule.train_dataloader_config.batch_size_per_device=4096 \
    data_loading.datamodule.train_dataloader_config.num_workers=16 \
    data_loading.datamodule.train_dataloader_config.pin_memory=true \
    data_loading.datamodule.train_dataloader_config.persistent_workers=true \
    seed=42 \
    trainer.max_epochs=null \
    trainer.max_steps=3000 \
    model.use_vcr_td=false \
    model.use_vcf=true \
    model.use_tcl=false \
    model.hamming_radius=2 \
    model.m_full=0.5 \
    model.m_partial=0.3 \
    model.lambda_full=0.2 \
    model.lambda_partial=0.1 \
    model.repulsion_warmup_steps=2000 \
    model.use_cvpm=true \
    model.cvpm_temperature=0.15 \
    model.exposure_counts_path=/home/pyy/GRID/src/data/amazon_data/beauty/exposure_counts.pt \
    model.use_dynamic_margin=false \
    model.use_time_decay=false \
    "hydra.run.dir=${run_root}/01_sid_train"
```

**SID 推理**:
```bash
CUDA_VISIBLE_DEVICES=1 python -m src.inference \
    experiment=rqvae_vcf_inference_flat \
    data_dir=/home/pyy/GRID/src/data/amazon_data/beauty \
    embedding_path=/home/pyy/GRID/embeddings/beauty/pickle/merged_predictions_tensor.pt \
    embedding_dim=768 \
    num_hierarchies=3 \
    codebook_width=256 \
    ckpt_path=<ckpt_path> \
    seed=42 \
    "hydra.run.dir=${run_root}/02_sid_inference"
```

**TIGER 训练**:
```bash
CUDA_VISIBLE_DEVICES=1 python -m src.train \
    experiment=tiger_train_flat \
    data_dir=/home/pyy/GRID/src/data/amazon_data/beauty \
    semantic_id_path=<sid_pt> \
    num_hierarchies=4 \
    seed=42 \
    "hydra.run.dir=${run_root}/03_tiger_train"
```

**脚本位置**: `scripts/run_C2_full_pipeline.sh`
**输出目录**: `outputs/C2_full_pipeline/C2_full_sid3000_20260430_013836/C2ctrl_LamRecalib/`
**日志目录**: `logs/C2_full_pipeline/C2_full_sid3000_20260430_013836/`

---

## 实验 D: C2a (C2ctrl + U-shape 时间权重)

**实验描述**: C2ctrl 基础上启用 use_time_decay + U-shape 权重

SID 训练命令与 C2ctrl 相同，额外添加:
```bash
    model.use_time_decay=true \
    +model.cold_pct=0.25 \
    +model.hot_pct=0.75 \
    +model.delta_cold=0.15 \
    +model.delta_hot=0.10
```

**脚本位置**: `scripts/run_C2_full_pipeline.sh`
**输出目录**: `outputs/C2_full_pipeline/C2_full_sid3000_20260430_013836/C2a_LamRecalib_LightUShape/`
**日志目录**: `logs/C2_full_pipeline/C2_full_sid3000_20260430_013836/`

---

## 实验 E: Path B Exp1 (非对称排斥, 无 CL)

**实验描述**: B0ctrl (对称) / B0 (α=0.05) / B0a (α=0.01), use_tcl=false

**SID 训练** (以 B0a 为例):
```bash
# 位于 scripts/run_pathB_experiment.sh 内
CUDA_VISIBLE_DEVICES=2 python -m src.train \
    experiment=rqvae_vcf_train_flat \
    data_dir=/home/pyy/GRID/src/data/amazon_data/beauty \
    embedding_path=/home/pyy/GRID/embeddings/beauty/pickle/merged_predictions_tensor.pt \
    embedding_dim=768 \
    num_hierarchies=3 \
    codebook_width=256 \
    data_loading.datamodule.train_dataloader_config.batch_size_per_device=4096 \
    data_loading.datamodule.train_dataloader_config.num_workers=16 \
    data_loading.datamodule.train_dataloader_config.pin_memory=true \
    data_loading.datamodule.train_dataloader_config.persistent_workers=true \
    seed=42 \
    trainer.max_epochs=null \
    trainer.max_steps=3000 \
    model.use_vcr_td=false \
    model.use_vcf=true \
    model.use_tcl=false \
    model.hamming_radius=2 \
    model.m_full=0.3 \
    model.m_partial=0.15 \
    model.lambda_full=0.2 \
    model.lambda_partial=0.1 \
    model.repulsion_warmup_steps=1000 \
    model.use_cvpm=true \
    model.cvpm_temperature=0.15 \
    model.exposure_counts_path=/home/pyy/GRID/src/data/amazon_data/beauty/exposure_counts.pt \
    model.use_dynamic_margin=false \
    model.use_time_decay=false \
    model.m_full_start=0.05 \
    model.m_partial_start=0.02 \
    model.margin_ramp_steps=2000 \
    model.use_asymmetric_repulsion=true \
    model.asymmetric_alpha_min=0.01 \
    model.asymmetric_temperature=2.0 \
    "hydra.run.dir=outputs/pathB/pathB_sid3000_20260505_234748/B0a/01_sid_train"
```

B0ctrl: `use_asymmetric_repulsion=false, asymmetric_alpha_min=0.0`
B0: `use_asymmetric_repulsion=true, asymmetric_alpha_min=0.05`

SID 推理和 TIGER 训练同 C2ctrl 模式。

**脚本位置**: `scripts/run_pathB_experiment.sh`
**输出目录**: `outputs/pathB/pathB_sid3000_20260505_234748/{B0ctrl,B0,B0a}/`
**日志目录**: `logs/pathB/pathB_sid3000_20260505_234748/`

---

## 实验 F: Path B Exp2 (非对称排斥 + v3 CL)

**实验描述**: 同 Exp1 但启用 use_tcl=true (v3 CL: τ=0.5, λ=0.001)

在 Exp1 基础上额外添加:
```bash
    model.use_tcl=true \
    model.quasid_cl_tau=0.5 \
    model.lambda_cl=0.001 \
    model.cl_ramp_steps=1000 \
    model.use_dual_tower=true
```

**脚本位置**: `scripts/run_pathB_experiment.sh`
**输出目录**: `outputs/pathB/pathB_sid3000_20260506_225320/{B0ctrl,B0,B0a}/`
**日志目录**: `logs/pathB/pathB_sid3000_20260506_225320/`

---

## 实验 G: DSF-Direct NPR (G0 k-NN + NPR)

**实验描述**: G0 预计算 k-NN + NPR α=0.01, CL on, 无 KPL

**SID 训练**:
```bash
# 位于 scripts/run_dsf_direct_full.sh 内
CUDA_VISIBLE_DEVICES=0 python -m src.train \
    experiment=rqvae_vcf_train_flat \
    data_dir=/home/pyy/GRID/src/data/amazon_data/beauty \
    embedding_path=/home/pyy/GRID/embeddings/beauty/pickle/merged_predictions_tensor.pt \
    embedding_dim=768 \
    num_hierarchies=3 \
    codebook_width=256 \
    data_loading.datamodule.train_dataloader_config.batch_size_per_device=2048 \
    data_loading.datamodule.train_dataloader_config.num_workers=12 \
    data_loading.datamodule.train_dataloader_config.pin_memory=true \
    data_loading.datamodule.train_dataloader_config.persistent_workers=true \
    seed=42 \
    trainer.max_epochs=null \
    trainer.max_steps=3000 \
    callbacks.model_checkpoint.every_n_train_steps=3000 \
    model.use_vcr_td=false \
    model.use_vcf=true \
    model.use_tcl=true \
    model.quasid_cl_tau=0.5 \
    model.lambda_cl=0.001 \
    model.cl_ramp_steps=1000 \
    model.hamming_radius=2 \
    model.m_full=0.3 \
    model.m_partial=0.15 \
    model.m_full_start=0.05 \
    model.m_partial_start=0.02 \
    model.margin_ramp_steps=2000 \
    model.lambda_full=1.0 \
    model.lambda_partial=0.5 \
    model.repulsion_warmup_steps=1000 \
    model.repulsion_soft_ramp_steps=0 \
    model.use_cvpm=true \
    model.cvpm_temperature=0.15 \
    model.exposure_counts_path=/home/pyy/GRID/src/data/amazon_data/beauty/exposure_counts.pt \
    model.use_dynamic_margin=false \
    model.use_time_decay=false \
    model.use_asymmetric_repulsion=false \
    model.margin_dissim_weight=0.3 \
    model.use_dual_tower=true \
    model.codebook_entropy_weight=0.1 \
    model.knn_g0_path=/home/pyy/GRID/embeddings/beauty/pickle/knn_g0_indices_k50.pt \
    model.use_npr=true \
    model.npr_alpha_min=0.01 \
    ++should_skip_retry=True \
    "hydra.run.dir=outputs/dsf_direct/dsf_direct_full_3000steps_20260511_210023/NPR/01_sid_train"
```

**脚本位置**: `scripts/run_dsf_direct_full.sh`
**输出目录**: `outputs/dsf_direct/dsf_direct_full_3000steps_20260511_210023/NPR/`
**TIGER 脚本**: `scripts/run_dsf_direct_tiger.sh`
**TIGER 输出**: `outputs/dsf_direct_tiger/dsf_direct_tiger_20260511_233143/NPR/`

---

## 实验 H: DSF-Direct KPL (KPL only)

**实验描述**: G0 预计算 k-NN + KPL loss, 无 NPR

同 NPR 命令，改为:
```bash
    model.use_npr=false \
    model.use_kpl=true \
    model.lambda_kpl=0.001
```

**脚本位置**: `scripts/run_dsf_direct_full.sh`
**输出目录**: `outputs/dsf_direct/dsf_direct_full_3000steps_20260511_210023/KPL/`

---

## 实验 I: DSF-Direct NPR+KPL

**实验描述**: NPR + KPL 组合

同 NPR 命令，改为:
```bash
    model.use_npr=true \
    model.use_kpl=true \
    model.lambda_kpl=0.001
```

**脚本位置**: `scripts/run_dsf_direct_full.sh`
**输出目录**: `outputs/dsf_direct/dsf_direct_full_3000steps_20260511_210023/NPR_KPL/`

---

## 实验 J: Online NPR (Online k-NN + NPR)

**实验描述**: EMA online k-NN 替代 G0 预计算 k-NN + NPR, 无 CL

**SID 训练**:
```bash
# 使用独立的 online-knn 配置文件 (configs/experiment/rqvae_vcf_online_knn_train_flat.yaml)
CUDA_VISIBLE_DEVICES=0 python -m src.train \
    experiment=rqvae_vcf_online_knn_train_flat \
    data_dir=/home/pyy/GRID/src/data/amazon_data/beauty \
    embedding_path=/home/pyy/GRID/embeddings/beauty/pickle/merged_predictions_tensor.pt \
    embedding_dim=768 \
    num_hierarchies=3 \
    codebook_width=256 \
    seed=42 \
    model.use_kpl=false \
    model.use_tcl=false \
    trainer.max_steps=3000 \
    callbacks.model_checkpoint.every_n_train_steps=3000 \
    optim.scheduler.warmup_steps=1000 \
    ++should_skip_retry=True \
    "hydra.run.dir=outputs/online_knn_npr_3000/01_sid_train"
```

⚠️ **注意**: 此命令未显式覆盖 `use_time_decay` 和 `use_dynamic_margin`，它们从配置文件继承了默认值 `true`。也未覆盖 `batch_size` (2048)、`repulsion_warmup` (200)、`lambda_full/lambda_partial` (1.0/0.5)。

**SID 推理**:
```bash
# 位于 scripts/run_online_npr_full_pipeline.sh 内
CUDA_VISIBLE_DEVICES=0 python -m src.inference \
    experiment=rqvae_vcf_inference_flat \
    data_dir=/home/pyy/GRID/src/data/amazon_data/beauty \
    embedding_path=/home/pyy/GRID/embeddings/beauty/pickle/merged_predictions_tensor.pt \
    num_hierarchies=3 \
    codebook_width=256 \
    embedding_dim=768 \
    ckpt_path=outputs/online_knn_npr_3000/01_sid_train/checkpoints/checkpoint_000_003000.ckpt \
    seed=42 \
    "hydra.run.dir=outputs/online_knn_npr_3000/02_sid_inference"
```

**TIGER 训练**:
```bash
# 位于 scripts/run_online_npr_full_pipeline.sh 内
CUDA_VISIBLE_DEVICES=0 python -m src.train \
    experiment=tiger_train_flat \
    data_dir=/home/pyy/GRID/src/data/amazon_data/beauty \
    semantic_id_path=outputs/online_knn_npr_3000/02_sid_inference/pickle/merged_predictions_tensor.pt \
    num_hierarchies=4 \
    seed=42 \
    trainer.max_steps=10000 \
    callbacks.model_checkpoint.save_last=true \
    "hydra.run.dir=outputs/online_knn_npr_3000/03_tiger_train"
```

**脚本位置**: SID 训练手动启动 | SID 推理+TIGER: `scripts/run_online_npr_full_pipeline.sh`
**配置文件**: `configs/experiment/rqvae_vcf_online_knn_train_flat.yaml`
**输出目录**: `outputs/online_knn_npr_3000/`
**日志目录**: `logs/online_knn_npr_3000/`

---

---

## 实验 O: Online NPR 对照实验 (NPR × HaMR 消融, 2026-05-13)

**实验描述**: 三组并行消融实验，共同底座 (batch=4096, λ=0.2/0.1, m=0.5/0.3, warmup=2000, 静态 margin)，对比 NPR + HaMR + CVPM 的组合效果。

**实验组**:
- E1 (GPU0): Online NPR + HaMR + CVPM (hamming_radius=2)
- E2 (GPU1): Online NPR, same-SID only (hamming_radius=0, 无 HaMR, 无 CVPM)
- C2ctrl\* (GPU2): HaMR + CVPM only (无 NPR, 修正静态 margin)

**运行方式**:
```bash
# 默认 3000 步 SID + 10000 步 TIGER
bash scripts/run_expr_online_npr_compare.sh

# 自定义步数
SID_MAX_STEPS=100 bash scripts/run_expr_online_npr_compare.sh
```

**脚本位置**: `scripts/run_expr_online_npr_compare.sh`
**通用配置文件**: `configs/experiment/rqvae_vcf_online_knn_train_flat.yaml` (所有 use_* 默认 false)
**SID 推理配置**: `configs/experiment/rqvae_vcf_inference_flat.yaml`
**TIGER 训练配置**: `configs/experiment/tiger_train_flat.yaml`
**输出目录**: `outputs/online_npr_compare/${RUN_TAG}/`
**日志目录**: `logs/online_npr_compare/${RUN_TAG}/`

**实验结果 (test/ndcg@10)**:

| 实验 | NDCG | DSF | L0 Codebook | 碰撞率 |
|------|:---:|:---:|:---:|:---:|
| E1 (NPR+HaMR+CVPM) | 0.02920 | 0.2592 | 51.17% | 8.69% |
| E2 (NPR, same-SID) | 0.02837 | 0.2751 | 26.17% | 13.74% |
| C2ctrl\* (HaMR+CVPM) | 0.02650 | 0.2528 | 31.64% | 14.50% |

---

## 实验 K: X1 代码本崩溃成因消融 (2026-05-14)

**实验描述**: 查明 X1 L0=33.6% 的原因。从 X1 底座 (batch=2048, warmup=200, NPR+online_knn, 无 TCL) 出发，消融预热时长和 TCL。

**运行方式** (通用流水线脚本):
```bash
# 默认 3 组并行 (GPU 0/1/2)
bash scripts/run_sid_pipeline.sh

# 自定义参数
N_GROUPS=2 GPU_LIST=(0 1) BATCH_SIZE=4096 bash scripts/run_sid_pipeline.sh

# 各组覆盖
G1_SPECS="model.repulsion_warmup_steps=1000" bash scripts/run_sid_pipeline.sh
```

**各组配置**:
- Exp1 (GPU0): batch=2048, warmup=200, 无 TCL (X1 底座复现)
- Exp2 (GPU1): batch=2048, warmup=1000, 无 TCL (隔离预热效果)
- Exp3 (GPU2): batch=2048, warmup=1000, TCL=true (隔离 TCL 效果)

**流水线脚本**: `scripts/run_sid_pipeline.sh`
**SID 训练配置**: `configs/experiment/rqvae_vcf_online_knn_train_flat.yaml`
**SID 推理配置**: `configs/experiment/rqvae_vcf_inference_flat.yaml`
**输出目录**: `outputs/sid_pipeline/sid_pipeline_20260514_192035/`
**日志目录**: `logs/sid_pipeline/sid_pipeline_20260514_192035/`

### 已修复的 Bug (流水线 v1 → v2)

| Bug | 原因 | 修复 |
|-----|------|------|
| Stdout 污染 | `run_sid_train` 通过 `tee` 输出日志到 stdout，但 `$()` 捕获全部 stdout 作为返回值。`ckpt_path` 收到多行日志文本 → Hydra 解析失败。 | 日志消息重定向到 `stderr` + 日志文件。仅返回值输出到 stdout。 |
| `_die` 短路 | `[[ ! -f ]] && _die && return 1` 模式：当 `_die` 返回 1（非零）时，`return 1` 因 `&&` 短路从未执行。 | 改用 `if ...; then _die; return 1; fi`。 |
| 推理状态字典不匹配 | 检查点包含 `online_knn_indices` buffer，但推理期间 `_init_online_knn_buffers` 由 `TrainerFn.PREDICTING` 门控跳过。PyTorch 严格加载失败。 | 在 `on_load_checkpoint` 中：若当前模型无此 buffer，从状态字典中去除该键。 |

---

## 实验 L: NPR+TCL 三组对比 — SID+TIGER 全流程 (2026-05-14)

**实验描述**: 统一底座 (batch=4096, warmup=1000, λ=1.0/0.5) 下对比 TCL_only / NPR_only / NPR_TCL 三组的 SID 训练 → SID 推理 → TIGER 训练完整流水线。

**运行方式**:
```bash
# 默认 3 组并行 (GPU 0/1/2), 3000 步 SID + 320000 步 TIGER
bash scripts/run_sid_tiger_pipeline.sh

# 自定义参数
RUN_TAG="expr_3g_sid3000_tiger" bash scripts/run_sid_tiger_pipeline.sh
```

**各组配置**:
- TCL_only (GPU0): use_tcl=true, use_npr=false, use_online_knn=false, use_dual_tower=true
- NPR_only (GPU1): use_tcl=false, use_npr=true, use_online_knn=true
- NPR_TCL (GPU2): use_tcl=true, use_npr=true, use_online_knn=true, use_dual_tower=true

**SID 训练** (以 NPR_TCL 为例):
```bash
# 位于 scripts/run_sid_tiger_pipeline.sh 内
CUDA_VISIBLE_DEVICES=2 python -m src.train \
    experiment=rqvae_vcf_online_knn_train_flat \
    data_dir=/home/pyy/GRID/src/data/amazon_data/beauty \
    embedding_path=/home/pyy/GRID/embeddings/beauty/pickle/merged_predictions_tensor.pt \
    embedding_dim=768 \
    num_hierarchies=3 \
    codebook_width=256 \
    data_loading.datamodule.train_dataloader_config.batch_size_per_device=4096 \
    data_loading.datamodule.train_dataloader_config.num_workers=12 \
    data_loading.datamodule.train_dataloader_config.pin_memory=true \
    data_loading.datamodule.train_dataloader_config.persistent_workers=true \
    seed=42 \
    trainer.max_epochs=null \
    trainer.max_steps=3000 \
    model.use_vcf=true \
    model.use_cvpm=true \
    model.hamming_radius=2 \
    model.lambda_full=1.0 \
    model.lambda_partial=0.5 \
    model.m_full=0.3 \
    model.m_partial=0.15 \
    model.m_full_start=0.05 \
    model.m_partial_start=0.02 \
    model.margin_ramp_steps=2000 \
    model.alpha=0.5 \
    model.exposure_counts_path=/home/pyy/GRID/src/data/amazon_data/beauty/exposure_counts.pt \
    model.repulsion_warmup_steps=1000 \
    model.cvpm_temperature=0.15 \
    model.use_time_decay=false \
    model.use_dynamic_margin=false \
    model.use_asymmetric_repulsion=false \
    model.use_tcl=true \
    model.use_dual_tower=true \
    model.use_kpl=false \
    model.lambda_cl=0.001 \
    model.quasid_cl_tau=0.5 \
    model.cl_ramp_steps=1000 \
    model.use_online_knn=true \
    model.use_npr=true \
    model.npr_alpha_min=0.01 \
    model.online_knn_k=50 \
    model.online_knn_ema_momentum=0.99 \
    model.online_knn_update_interval=100 \
    callbacks.model_checkpoint.every_n_train_steps=3000 \
    optim.scheduler.warmup_steps=1000 \
    ++should_skip_retry=true \
    "hydra.run.dir=${run_root}/01_sid_train"
```

**SID 推理**:
```bash
# 位于 scripts/run_sid_tiger_pipeline.sh 内
CUDA_VISIBLE_DEVICES=2 python -m src.inference \
    experiment=rqvae_vcf_inference_flat \
    data_dir=/home/pyy/GRID/src/data/amazon_data/beauty \
    embedding_path=/home/pyy/GRID/embeddings/beauty/pickle/merged_predictions_tensor.pt \
    embedding_dim=768 \
    num_hierarchies=3 \
    codebook_width=256 \
    ckpt_path=<ckpt_path> \
    seed=42 \
    "hydra.run.dir=${run_root}/02_sid_inference"
```

**TIGER 训练**:
```bash
# 位于 scripts/run_sid_tiger_pipeline.sh 内
CUDA_VISIBLE_DEVICES=2 python -m src.train \
    experiment=tiger_train_flat \
    data_dir=/home/pyy/GRID/src/data/amazon_data/beauty \
    semantic_id_path=<sid_pt> \
    num_hierarchies=4 \
    seed=42 \
    trainer.max_steps=320000 \
    callbacks.model_checkpoint.save_last=true \
    "hydra.run.dir=${run_root}/03_tiger_train"
```

**⚠️ 已知问题**: 
- TIGER 推理步骤 (04) 因 Hydra 无法解析 checkpoint 路径中的 `=` 字符而失败，不影响 SID + TIGER 训练指标
- TIGER checkpoint 路径包含 `=` 需用单引号包裹：`"ckpt_path='${tiger_ckpt}'"`（已在脚本中修复，但步骤 04 仍失败，根因可能在 Hydra 的 override 解析层）

**流水线脚本**: `scripts/run_sid_tiger_pipeline.sh`
**SID 训练配置**: `configs/experiment/rqvae_vcf_online_knn_train_flat.yaml`
**SID 推理配置**: `configs/experiment/rqvae_vcf_inference_flat.yaml`
**TIGER 训练配置**: `configs/experiment/tiger_train_flat.yaml`
**输出目录**: `outputs/sid_tiger/expr_3g_sid3000_tiger/{TCL_only,NPR_only,NPR_TCL}/`
**日志目录**: `logs/sid_tiger/expr_3g_sid3000_tiger/`

**实验结果 (test/ndcg@10)**:

| 实验 | NDCG@10 | recall@10 | L0 Cov | 部分碰撞率 |
|------|:---:|:---:|:---:|:---:|
| NPR_TCL | **0.0335** | **0.0604** | **100%** | **2.43%** |
| NPR_only | 0.0267 | 0.0458 | 37.9% | 4.05% |
| TCL_only | 0.0260 | 0.0459 | 50.8% | 3.73% |

---

## 实验 M: QuaSID 消融 — QuaSID复现 vs QuaSID+NPR+dynM (2026-05-16)

**实验描述**: 两组并行：Exp1 (GPU1) 使用 QuaSID 原版参数 (λ=0.2/0.1, m=0.8/0.5, R=1, λ_cl=0.1) 复现 TCL_only；Exp2 (GPU2) 在 NPR+TCL (3g) 底座上添加 dynamic_margin (m0=0.8, margin_dissim_weight=0.3)。

**运行方式**:
```bash
# 默认 3000 步 SID + 320000 步 TIGER (两组并行: GPU1/GPU2)
bash scripts/run_quasid_ablation.sh

# Smoke test (50 步)
SID_MAX_STEPS=50 TIGER_MAX_STEPS=50 bash scripts/run_quasid_ablation.sh
```

**各组配置**:
- Exp1 (GPU1, QuaSID_TCL): λ_full=0.2, λ_partial=0.1, m_full=0.8, m_partial=0.5, hamming_radius=1, λ_cl=0.1, cl_ramp=0, 静态 margin 无 ramp, warmup=1000, use_npr=false
- Exp2 (GPU2, QuaSID_NPR_dynM): 3g 底座 + use_dynamic_margin=true, m0=0.8, margin_dissim_weight=0.3, warmup=1000

**SID 训练公共底座**:
```bash
# 位于 scripts/run_quasid_ablation.sh 内 (SID_COMMON_ARGS)
experiment=rqvae_vcf_online_knn_train_flat
data_dir="${DATA_DIR}"
embedding_path="${EMB_PATH}"
embedding_dim="${EMB_DIM}"
num_hierarchies=3
codebook_width=256
seed=42
trainer.max_steps=3000
model.use_vcf=true
model.use_cvpm=true
model.cvpm_temperature=0.15
model.alpha=0.5
model.repulsion_warmup_steps=1000
callbacks.model_checkpoint.every_n_train_steps=3000
optim.scheduler.warmup_steps=1000
```

**SID 推理**:
```bash
# 位于 scripts/run_quasid_ablation.sh 内
python -m src.inference \
    experiment=rqvae_vcf_inference_flat \
    data_dir="${DATA_DIR}" \
    embedding_path="${EMB_PATH}" \
    embedding_dim=768 \
    num_hierarchies=3 \
    codebook_width=256 \
    ckpt_path=<ckpt_path> \
    seed=42 \
    "hydra.run.dir=${run_root}/02_sid_inference"
```

**TIGER 训练**:
```bash
# 位于 scripts/run_quasid_ablation.sh 内
python -m src.train \
    experiment=tiger_train_flat \
    data_dir="${DATA_DIR}" \
    semantic_id_path=<sid_pt> \
    num_hierarchies=4 \
    seed=42 \
    trainer.max_steps=320000 \
    trainer.val_check_interval=1600 \
    trainer.accumulate_grad_batches=16 \
    optim.optimizer.lr=0.001 \
    optim.optimizer.weight_decay=0.0001 \
    "hydra.run.dir=${run_root}/03_tiger_train"
```

**⚠️ 已知问题**: TIGER checkpoint 因 `every_n_train_steps=320000` 覆盖导致 PL `_every_n_epochs=0` 而未保存（已修复：移除 TIGER `every_n_train_steps` 覆盖）。指标直接从 TIGER 日志提取。

**脚本位置**: `scripts/run_quasid_ablation.sh`
**SID 训练配置**: `configs/experiment/rqvae_vcf_online_knn_train_flat.yaml`
**SID 推理配置**: `configs/experiment/rqvae_vcf_inference_flat.yaml`
**TIGER 训练配置**: `configs/experiment/tiger_train_flat.yaml`
**输出目录**: `outputs/quasid_ablation/${RUN_TAG}/`
**日志目录**: `logs/quasid_ablation/${RUN_TAG}/`

**实验结果 (test/ndcg@10)**:

| 实验 | NDCG@10 | recall@10 | L0 Cov | 部分碰撞率 |
|------|:---:|:---:|:---:|:---:|
| QuaSID_NPR_dynM | **0.0320** | **0.0563** | **53.5%** | **3.32%** |
| QuaSID_TCL | 0.0301 | 0.0535 | 33.2% | 3.86% |

---

## 实验 N: TCL λ_cl/Batch/τ Sweep — 找稳定码本参数

**目的**: 找到使 TCL_only 码本稳定（L0>70%）的参数，作为与 NPR 公平对比的 baseline。

**脚本位置**: `scripts/run_tcl_lambda_sweep.sh`

**使用方法**:
```bash
# 直接运行 (默认 batch 和各组参数在脚本顶部定义)
bash scripts/run_tcl_lambda_sweep.sh

# 自定义 RUN_TAG
RUN_TAG=my_tcl_test bash scripts/run_tcl_lambda_sweep.sh
```

**脚本架构**: GPU1/GPU2 并行跑两组。每组：SID 训练 (400步) → SID 推理 → 碰撞分析。支持 per-group override（通过 `G1_OVERRIDES`/`G2_OVERRIDES` 数组）。

**Sweep 历程**:

| Phase | 变量 | GPU1 | GPU2 | 关键发现 |
|-------|------|------|------|---------|
| 1 | λ_cl | 0.01 | 0.05 | L0 天花板~40%，λ_cl 非单调 |
| 2 | λ_cl | 0.1 | 0.3 | λ_cl=0.3 碰撞率暴涨 |
| 3 | 轻底座 | λ_cl=0.05 | λ_cl=0.1 | 降低 repulsion 反降 L0 |
| 4 | Batch | B=256 | B=1024 | **B=256 L0 突破 73%！** |
| 5 | B256 QuaSID vs B512 | QuaSID+Adagrad | B512+强底座 | B512 L0=75.8% |
| 6 | 优化器 | QuaSID+Adam | B512+强底座 | **Adam L0=94.1%！** |
| 7 | τ | 0.1 | 0.3 | 低 τ 崩塌 |
| 8 | τ | 0.7 | 1.0 | τ=0.7 L0=94.9% |
| 9 | τ | 0.4 | 0.6 | τ=0.4 优秀，0.6 异常 |
| 10 | τ | 0.2 | 0.8 | 边界确认 |
| 11 | τ 复验 | 0.5 | 0.7 | **τ=0.5 最稳定** |

**最终 TCL_only Stable Baseline**:
```bash
BATCH_SIZE=256
model.hamming_radius=1
model.lambda_full=0.2 model.lambda_partial=0.1
model.m_full=0.8 model.m_partial=0.5  # 静态 margin (无 ramp)
model.lambda_cl=0.1
model.quasid_cl_tau=0.5
optim.optimizer._target_=torch.optim.Adam
optim.optimizer.lr=0.0003
optim.optimizer.weight_decay=0.00001
# → L0=91-94%, unique SIDs=92%
```

**SID 训练配置**: `configs/experiment/rqvae_vcf_online_knn_train_flat.yaml`
**SID 推理配置**: `configs/experiment/rqvae_vcf_inference_flat.yaml`
**输出目录**: `outputs/tcl_sweep/${RUN_TAG}/`
**日志目录**: `logs/tcl_sweep/${RUN_TAG}/`

---

## 实验 N.5: NPR+TCL α_min Sweep — NPR 保护强度对码本影响 (2026-05-18)

**实验描述**: 在 NPR+TCL QuaSID base (batch=256, R=1, λ=0.2/0.1, m=0.8/0.5 static, Adam) 上扫描 NPR α_min ∈ [0.001, 1.0]，观察 NPR 保护强度对码本利用率的影响。

**运行方式**:
```bash
# 编辑 scripts/run_tcl_lambda_sweep.sh 内的 G1_OVERRIDES/G2_OVERRIDES 切换 α_min
bash scripts/run_tcl_lambda_sweep.sh
```

**底座参数**: 与实验 N 最终 baseline 相同 (QuaSID base + NPR + TCL)

**各组配置** (共用 COMMON_OVERRIDES，仅 npr_alpha_min 不同):

| α_min | 含义 |
|:-----:|------|
| 0.001 | 极端保护 (几乎不推 k-NN 邻居) |
| 0.01 | 强保护 |
| 0.05 | 中等保护 |
| 0.1 | 弱保护 |
| 0.5 | 很弱保护 |
| 1.0 | 无保护 (对称排斥，等效于 NPR 关闭) |

**扫参结果**:

| α_min | frac_unique | colliding% | L0 cov | L1 cov | L2 cov | L0 ent | partial_col |
|:-----:|:----------:|:----------:|:------:|:------:|:------:|:------:|:-----------:|
| 0.001 | 0.918 | 14.2% | 0.938 | 0.563 | 0.246 | 0.928 | 4.05% |
| 0.01 | 0.886 | 19.2% | 0.938 | 0.582 | 0.238 | 0.929 | 5.11% |
| 0.05 | 0.925 | 13.3% | 0.902 | 0.582 | 0.273 | 0.917 | 3.60% |
| 0.1 | 0.903 | 16.9% | 0.898 | 0.520 | 0.262 | 0.920 | 4.49% |
| 0.5 | 0.901 | 17.5% | 0.848 | 0.520 | 0.285 | 0.909 | 4.50% |
| **1.0** | **0.942** | **10.2%** | **0.957** | **0.652** | **0.324** | **0.933** | **3.26%** |

**关键发现**: α_min=1.0 (无保护) 码本利用率最高。越强的 NPR 保护 → 码本越不稳定。NPR 在"码本区分能力"和"DSF 保真度"之间做权衡。

**日志目录**: `logs/tcl_sweep/tcl_sweep_20260518_{005755,010443,010926}/`

---

## 实验 O: KPL Sweep — NPR+TCL+KPL λ_kpl/excl_r 扫参 (2026-05-18)

**实验描述**: 在 NPR+TCL (QuaSID base, α=0.01) 底座上添加 KPL loss，扫描 λ_kpl 和 collision_exclude_radius 找码本稳定参数。

**运行方式**:
```bash
# 默认 400 步 SID smoke test (2 组并行: GPU1/GPU2)
bash scripts/run_kpl_sweep.sh

# 自定义 RUN_TAG
RUN_TAG=kpl_my_test bash scripts/run_kpl_sweep.sh
```

**脚本架构**: GPU1/GPU2 并行。共用底座: QuaSID(R=1,λ=0.2/0.1,m=0.8/0.5 static,Adam 3e-4/1e-5) + NPR(α=0.01) + TCL(λ_cl=0.1, τ=0.5)。编辑脚本顶部 `G1_*`/`G2_*` 变量切换扫参目标。

**各组配置**:
- SID_COMMON_BASE: batch=256, use_npr=true, use_tcl=true, use_kpl=true, kpl_ramp_steps=0
- COMMON_OVERRIDES: QuaSID VCF params + NPR(α=0.01, online_knn_k=50) + TCL(λ_cl=0.1, τ=0.5)

**扫参历程**:

| Phase | λ_kpl | excl_r | frac_unique | L0 cov | L1 cov | 结论 |
|:-----:|------:|:------:|:----------:|:------:|:------:|------|
| 1 | 0.001 | 0 | **0.926** | **0.953** | **0.691** | 最佳 |
| 1 | 0.01 | 0 | 0.866 | 0.918 | 0.543 | 过强，冲突 |
| 2 | 0.0001 | 0 | 0.854→0.901 | 0.961 | 0.691 | 有波动，不如 0.001 |
| 2 | 0.0005 | 0 | 0.882 | 0.906 | 0.535 | 不如 0.001 |
| 3 | 0.0001 v2 | 0 | 0.901 | 0.961 | 0.691 | 验证波动 |
| 3 | 0.1 | 0 | 0.578 | 0.797 | 0.211 | 码本崩塌 |
| 4 | 0.001 | 2 | 0.890 | 0.910 | 0.555 | excl_r=2 反而差 |
| 4 | 0.01 | 2 | 0.908 | 0.953 | 0.645 | excl_r=2 救回高 λ |

**最终最佳配置**: λ_kpl=0.001, excl_r=0 → frac_unique=0.926, L0 cov=0.953

**SID 训练配置**: `configs/experiment/rqvae_vcf_online_knn_train_flat.yaml`
**SID 推理配置**: `configs/experiment/rqvae_vcf_inference_flat.yaml`
**输出目录**: `outputs/kpl_sweep/${RUN_TAG}/`
**日志目录**: `logs/kpl_sweep/`
**脚本位置**: `scripts/run_kpl_sweep.sh`

---

## 实验 P: 4-Group Final Comparison (SID=1000 + TIGER 早停, 2026-05-18)

**实验描述**: 统一底座 (batch=256, R=1, λ=0.2/0.1, m=0.8/0.5 static, Adam, warmup=0) 下 4 组全流程对比。Phase 1: GPU 0/1/2 并行跑 TCL_only / NPR+TCL / NPR+TCL+KPL；Phase 2: GPU 0 跑 G0 (纯 RQ-VAE)。

**Phase 1 — SID 训练** (以 NPR+TCL+KPL 为例，由 run_sid_tiger_pipeline.sh 调度):
```bash
CUDA_VISIBLE_DEVICES=2 python -m src.train \
    experiment=rqvae_vcf_online_knn_train_flat \
    data_dir=/home/pyy/GRID/src/data/amazon_data/beauty \
    embedding_path=/home/pyy/GRID/embeddings/beauty/pickle/merged_predictions_tensor.pt \
    embedding_dim=768 \
    num_hierarchies=3 \
    codebook_width=256 \
    data_loading.datamodule.train_dataloader_config.batch_size_per_device=256 \
    data_loading.datamodule.train_dataloader_config.num_workers=12 \
    data_loading.datamodule.train_dataloader_config.pin_memory=true \
    data_loading.datamodule.train_dataloader_config.persistent_workers=true \
    seed=42 \
    trainer.max_epochs=null \
    trainer.max_steps=1000 \
    callbacks.model_checkpoint.every_n_train_steps=1000 \
    model.use_vcf=true \
    model.use_cvpm=true \
    model.hamming_radius=1 \
    model.lambda_full=0.2 \
    model.lambda_partial=0.1 \
    model.m_full=0.8 \
    model.m_partial=0.5 \
    model.alpha=0.5 \
    model.exposure_counts_path=/home/pyy/GRID/src/data/amazon_data/beauty/exposure_counts.pt \
    model.repulsion_warmup_steps=0 \
    model.cvpm_temperature=0.15 \
    model.use_time_decay=false \
    model.use_dynamic_margin=false \
    model.use_asymmetric_repulsion=false \
    model.use_tcl=true \
    model.use_dual_tower=true \
    model.use_kpl=true \
    model.lambda_kpl=0.001 \
    model.kpl_collision_exclude_radius=0 \
    model.lambda_cl=0.1 \
    model.quasid_cl_tau=0.5 \
    model.cl_ramp_steps=0 \
    model.use_online_knn=true \
    model.use_npr=true \
    model.npr_alpha_min=0.01 \
    model.online_knn_k=50 \
    model.online_knn_ema_momentum=0.99 \
    model.online_knn_update_interval=100 \
    model.codebook_entropy_weight=0.1 \
    optim.scheduler.warmup_steps=0 \
    optim.optimizer._target_=torch.optim.Adam \
    optim.optimizer.lr=0.0003 \
    optim.optimizer.weight_decay=0.00001 \
    ++should_skip_retry=true \
    "hydra.run.dir=${run_root}/01_sid_train"
```

各组差异:
- TCL_only: `use_npr=false, use_online_knn=false, use_kpl=false`
- NPR_TCL: `use_kpl=false`
- NPR_TCL_KPL: 完整参数如上

**Phase 1 — SID 推理**:
```bash
CUDA_VISIBLE_DEVICES=2 python -m src.inference \
    experiment=rqvae_vcf_inference_flat \
    data_dir=/home/pyy/GRID/src/data/amazon_data/beauty \
    embedding_path=/home/pyy/GRID/embeddings/beauty/pickle/merged_predictions_tensor.pt \
    embedding_dim=768 \
    num_hierarchies=3 \
    codebook_width=256 \
    ckpt_path=<ckpt_path> \
    seed=42 \
    "hydra.run.dir=${run_root}/02_sid_inference"
```

**Phase 1 — TIGER 训练**:
```bash
CUDA_VISIBLE_DEVICES=2 python -m src.train \
    experiment=tiger_train_flat \
    data_dir=/home/pyy/GRID/src/data/amazon_data/beauty \
    semantic_id_path=<sid_pt> \
    num_hierarchies=4 \
    seed=42 \
    trainer.max_steps=320000 \
    trainer.val_check_interval=1600 \
    trainer.accumulate_grad_batches=16 \
    optim.optimizer.lr=0.001 \
    optim.optimizer.weight_decay=0.0001 \
    callbacks.model_checkpoint.save_last=true \
    "hydra.run.dir=${run_root}/03_tiger_train"
```

**Phase 2 — G0 SID 训练** (纯 RQ-VAE, 所有开关关闭):
```bash
CUDA_VISIBLE_DEVICES=0 python -m src.train \
    experiment=rqvae_vcf_online_knn_train_flat \
    data_dir=/home/pyy/GRID/src/data/amazon_data/beauty \
    embedding_path=/home/pyy/GRID/embeddings/beauty/pickle/merged_predictions_tensor.pt \
    embedding_dim=768 \
    num_hierarchies=3 \
    codebook_width=256 \
    data_loading.datamodule.train_dataloader_config.batch_size_per_device=256 \
    data_loading.datamodule.train_dataloader_config.num_workers=12 \
    data_loading.datamodule.train_dataloader_config.pin_memory=true \
    data_loading.datamodule.train_dataloader_config.persistent_workers=true \
    seed=42 \
    trainer.max_epochs=null \
    trainer.max_steps=1000 \
    callbacks.model_checkpoint.every_n_train_steps=1000 \
    optim.scheduler.warmup_steps=0 \
    optim.optimizer._target_=torch.optim.Adam \
    optim.optimizer.lr=0.0003 \
    optim.optimizer.weight_decay=0.00001 \
    model.use_vcf=false \
    model.use_cvpm=false \
    model.use_tcl=false \
    model.use_npr=false \
    model.use_kpl=false \
    model.use_dual_tower=false \
    model.use_online_knn=false \
    model.repulsion_warmup_steps=0 \
    ++should_skip_retry=true \
    "hydra.run.dir=${G0_OUT_ROOT}/G0/01_sid_train"
```

**脚本位置**: `scripts/run_final_4groups.sh`
**Phase 1 流水线**: `scripts/run_sid_tiger_pipeline.sh`
**SID 训练配置**: `configs/experiment/rqvae_vcf_online_knn_train_flat.yaml`
**SID 推理配置**: `configs/experiment/rqvae_vcf_inference_flat.yaml`
**TIGER 训练配置**: `configs/experiment/tiger_train_flat.yaml`
**输出目录**: `outputs/final_4g/final_4g_20260518_034823/`
**日志目录**: `logs/final_4g/final_4g_20260518_034823/`

**实验结果 (test/ndcg@10)**:

| 实验 | NDCG@10 | recall@10 | ndcg+hr@10 | L0 Cov | frac_unique | 部分碰撞率 |
|------|:---:|:---:|:---:|:---:|:---:|:---:|
| TCL_only | 0.0341 | 0.0603 | 0.0944 | 97.3% | 0.962 | 2.81% |
| **NPR_TCL** | **0.0355** | **0.0611** | **0.0966** | **91.0%** | **0.948** | **3.09%** |
| NPR_TCL_KPL | 0.0321 | 0.0584 | 0.0905 | 84.8% | 0.941 | 3.03% |
| G0 (纯RQ-VAE) | 0.0393 ⚠️ | 0.0642 ⚠️ | 0.1034 ⚠️ | 0.4% | ~0 | N/A |

⚠️ **G0 码本完全崩塌** (frac_unique=0.00008, 仅1个唯一SID, L0/L1/L2 各仅1个token)。G0 TIGER 的 NDCG 来自 layer3 直接记忆 item ID 而非 SID 泛化，**不可作为有效基线**。

---

## 实验 Q: RNCL (Reciprocal Neighbor Consistency Loss, 2026-05-19)

**实验描述**: 在 NPR+TCL 4g 底座上添加 RNCL loss（λ=5e-6），验证互惠 k-NN 余弦距离吸引力对 SID+TIGER 的效果。RNCL 仅作用于双向 k-NN 对（reciprocal pairs），使用 `mean(1-cos_sim)` 纯吸引力损失，极小权重避免与 VCF 冲突。

**λ Sweep** (6 个 λ 值, 400步 smoke test, 3 rounds):
```bash
# ROUND=0: λ=1e-6, 5e-6 (GPU 1/2)
# ROUND=1: λ=1e-5, 3e-5 (GPU 1/2)
# ROUND=2: λ=1e-4, 3e-4 (GPU 1/2)
ROUND=0 bash scripts/run_rncl_lambda_sweep.sh
ROUND=1 bash scripts/run_rncl_lambda_sweep.sh
ROUND=2 bash scripts/run_rncl_lambda_sweep.sh
```

**λ Sweep 结果** (400步 smoke):

| λ_rncl | frac_unique | colliding% | L0 cov | L1 cov | L2 cov | max_col | 结论 |
|:------:|:----------:|:----------:|:------:|:------:|:------:|:-------:|------|
| 1e-6 | 0.930 | 12.0% | 0.922 | 0.609 | 0.281 | 5 | ✅ 安全 |
| **5e-6** | **0.922** | **13.3%** | **0.918** | **0.594** | **0.270** | **5** | **✅ 安全** |
| 1e-5 | 0.896 | 17.3% | 0.895 | 0.559 | 0.246 | 8 | ⚠️ 边缘 |
| 3e-5 | 0.859 | 22.7% | 0.863 | 0.461 | 0.219 | 13 | ⚠️ 边缘 |
| 1e-4 | 0.679 | 44.3% | 0.727 | 0.234 | 0.113 | 37 | ❌ 崩塌 |
| 3e-4 | 0.246 | 79.6% | 0.457 | 0.066 | 0.047 | 187 | ❌ 严重崩塌 |

**Full Experiment** (λ=5e-6, 1000步 SID + TIGER):
```bash
# GPU 2, 使用 scripts/run_rncl_full_experiment.sh
RUN_TAG=rncl_full_$(date +%Y%m%d_%H%M%S) bash scripts/run_rncl_full_experiment.sh

# 自定义参数
SID_MAX_STEPS=400 TIGER_MAX_STEPS=10 TIGER_VAL_INTERVAL=5 bash scripts/run_rncl_full_experiment.sh
```

**SID 训练** (以 NPR_TCL_RNCL 为例，由 run_sid_tiger_pipeline.sh 调度):
```bash
CUDA_VISIBLE_DEVICES=2 python -m src.train \
    experiment=rqvae_vcf_online_knn_train_flat \
    data_dir=/home/pyy/GRID/src/data/amazon_data/beauty \
    embedding_path=/home/pyy/GRID/embeddings/beauty/pickle/merged_predictions_tensor.pt \
    embedding_dim=768 \
    num_hierarchies=3 \
    codebook_width=256 \
    data_loading.datamodule.train_dataloader_config.batch_size_per_device=256 \
    data_loading.datamodule.train_dataloader_config.num_workers=12 \
    data_loading.datamodule.train_dataloader_config.pin_memory=true \
    data_loading.datamodule.train_dataloader_config.persistent_workers=true \
    seed=42 \
    trainer.max_epochs=null \
    trainer.max_steps=1000 \
    callbacks.model_checkpoint.every_n_train_steps=1000 \
    model.use_vcf=true \
    model.use_cvpm=true \
    model.hamming_radius=1 \
    model.lambda_full=0.2 \
    model.lambda_partial=0.1 \
    model.m_full=0.8 \
    model.m_partial=0.5 \
    model.alpha=0.5 \
    model.exposure_counts_path=/home/pyy/GRID/src/data/amazon_data/beauty/exposure_counts.pt \
    model.repulsion_warmup_steps=0 \
    model.cvpm_temperature=0.15 \
    model.use_time_decay=false \
    model.use_dynamic_margin=false \
    model.use_asymmetric_repulsion=false \
    model.use_tcl=true \
    model.use_dual_tower=true \
    model.use_kpl=false \
    model.lambda_cl=0.1 \
    model.quasid_cl_tau=0.5 \
    model.cl_ramp_steps=0 \
    model.use_online_knn=true \
    model.use_npr=true \
    model.npr_alpha_min=0.01 \
    model.online_knn_k=50 \
    model.online_knn_ema_momentum=0.99 \
    model.online_knn_update_interval=100 \
    model.use_rncl=true \
    model.lambda_rncl=0.000005 \
    model.rncl_ramp_steps=500 \
    model.codebook_entropy_weight=0.1 \
    optim.scheduler.warmup_steps=0 \
    optim.optimizer._target_=torch.optim.Adam \
    optim.optimizer.lr=0.0003 \
    optim.optimizer.weight_decay=0.00001 \
    ++should_skip_retry=true \
    "hydra.run.dir=${run_root}/01_sid_train"
```

**SID 推理** (同其他实验):
```bash
CUDA_VISIBLE_DEVICES=2 python -m src.inference \
    experiment=rqvae_vcf_inference_flat \
    data_dir=/home/pyy/GRID/src/data/amazon_data/beauty \
    embedding_path=/home/pyy/GRID/embeddings/beauty/pickle/merged_predictions_tensor.pt \
    embedding_dim=768 \
    num_hierarchies=3 \
    codebook_width=256 \
    ckpt_path=<ckpt_path> \
    seed=42 \
    "hydra.run.dir=${run_root}/02_sid_inference"
```

**TIGER 训练** (同其他实验):
```bash
CUDA_VISIBLE_DEVICES=2 python -m src.train \
    experiment=tiger_train_flat \
    data_dir=/home/pyy/GRID/src/data/amazon_data/beauty \
    semantic_id_path=<sid_pt> \
    num_hierarchies=4 \
    seed=42 \
    trainer.max_steps=320000 \
    trainer.val_check_interval=1600 \
    trainer.accumulate_grad_batches=16 \
    optim.optimizer.lr=0.001 \
    optim.optimizer.weight_decay=0.0001 \
    callbacks.model_checkpoint.save_last=true \
    "hydra.run.dir=${run_root}/03_tiger_train"
```

**脚本位置**: 
- λ sweep: `scripts/run_rncl_lambda_sweep.sh`
- 全实验: `scripts/run_rncl_full_experiment.sh`
- 流水线: `scripts/run_sid_tiger_pipeline.sh`

**SID 训练配置**: `configs/experiment/rqvae_vcf_online_knn_train_flat.yaml`
**RNCL 实现**: `src/modules/clustering/knn_preservation.py` (`build_reciprocal_mask`, `compute_rncl_loss`)
**SID 推理配置**: `configs/experiment/rqvae_vcf_inference_flat.yaml`
**TIGER 训练配置**: `configs/experiment/tiger_train_flat.yaml`
**输出目录**: `outputs/rncl_full/rncl_full_20260519_035957/`
**日志目录**: `logs/rncl_full/rncl_full_20260519_035957/`
**DSF 分析**: `logs/rncl_full/rncl_full_20260519_035957/dsf_analysis.{txt,npz}`

**实验结果 (test/ndcg@10)**:

| 实验 | NDCG@10 | recall@10 | ndcg+hr@10 | L0 Cov | DSF_online | max_col | vs Baseline |
|------|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| NPR+TCL (baseline) | **0.0355** | **0.0611** | **0.0966** | **91.0%** | **0.175** | **7** | — |
| NPR+TCL+RNCL | 0.0322 | 0.0591 | 0.0913 | 89.1% | 0.168 | 49 | **−9.3%** |

**关键发现**: RNCL 在 λ=5e-6 极小权重下仍为负效果。原因：RNCL 与 TCL InfoNCE 信号高度冗余（互惠 k-NN 对 ≈ 共现对），纯吸引力 cosine distance 无排斥分量导致 codebook 挤压（max_collision=49），DSF_online 降低（0.168）→ TIGER NDCG 下降。DSF→NDCG 因果链再次验证。

---

## 附录: 脚本文件清单

| 脚本 | 用途 |
|------|------|
| `scripts/run_G0_only_gpu1.sh` | G0 纯 RQ-VAE 全流程 |
| `scripts/run_G0_G2_ablation.sh` | G0/G2 消融实验 |
| `scripts/run_final_3groups_full_pipeline.sh` | Q0/Q0+B1 最终三组对比 |
| `scripts/run_C2_full_pipeline.sh` | C2ctrl/C2a λ 调优 + U-shape |
| `scripts/run_pathB_experiment.sh` | Path B 非对称排斥 (Exp1 & Exp2) |
| `scripts/run_dsf_direct_full.sh` | DSF-Direct NPR/KPL/NPR+KPL SID 训练 |
| `scripts/run_dsf_direct_tiger.sh` | DSF-Direct TIGER 训练+推理 |
| `scripts/run_expr_online_npr_compare.sh` | Online NPR 对照实验 (NPR×HaMR 消融) |
| `scripts/run_online_npr_full_pipeline.sh` | Online NPR SID 推理 + TIGER 训练 |
| `scripts/run_sid_pipeline.sh` | **通用 SID 训练+推理流水线 (控制台参数化)** |
| `scripts/run_sid_tiger_pipeline.sh` | **通用 SID 训练+推理+TIGER 训练流水线 (控制台参数化)** |
| `scripts/run_quasid_ablation.sh` | **QuaSID 消融实验: 复现 vs dynM 对比** |
| `scripts/run_tcl_lambda_sweep.sh` | **TCL_only λ_cl/Batch/τ sweep 找稳定码本** |
| `scripts/run_kpl_sweep.sh` | **KPL λ_kpl/excl_r sweep (NPR+TCL+KPL)** |
| `scripts/run_rncl_lambda_sweep.sh` | **RNCL λ sweep (NPR+TCL+RNCL, 6 λ values)** |
| `scripts/run_rncl_full_experiment.sh` | **RNCL Full Experiment (λ=5e-6, SID+TIGER)** |
| `scripts/analyze_sid_collisions.py` | SID 碰撞分析脚本 |
| `scripts/analyze_dsf.py` | DSF 分析脚本 |
| `scripts/analyze_dsf_direct.py` | DSF-Direct DSF 分析脚本 |
| `scripts/analyze_ecrd.py` | ECRD 漂移分析脚本 |
