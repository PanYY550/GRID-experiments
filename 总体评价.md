代码读完了。以下是完全基于代码实现的精确审查结果，对 Kimi 的核实进行确认和补充。

---

## Kimi 核实结论：完全正确的部分

先正式确认 Kimi 的三项核实是对的，我原来的说法基于文档伪代码，确实与实际实现不符：

| 我原来报的问题 | 实际代码 | 结论 |
|---|---|---|
| 问题 3：R 未传参 | 第399行 `self.hamming_radius` 通过实例属性访问 | ✅ Kimi 正确，不是 bug |
| 问题 7：min_margin/max_enhance 未实现 | 第378行 `clamp(min=self.min_margin)`，第413行 `clamp(max=self.max_enhance)` | ✅ Kimi 正确，已实现 |
| 问题 8：warmup 未实现 | 第871-873行已有 warmup_factor 逻辑 | ✅ Kimi 正确，已实现 |

---

## Kimi 遗漏的真实 Bug（代码层面）

### 🔴 Bug 1（最严重）：`condition` 缺少 `use_vcf`，导致 VCF 核心路径被永久跳过

```795:795:src/modules/clustering/residual_quantization.py
condition = (self.use_vcr_td or self.use_tcl) and self.trainer.state.fn != TrainerFn.PREDICTING
```

而 `vcf_repulsion_loss` 调用在这个 `condition` 的 `if` 块**内部**：

```859:875:src/modules/clustering/residual_quantization.py
            # === VCF (VCR-TD-QuaSID Fusion) 排斥损失 ===
            if self.use_vcf:
                vcf_loss, n_full, n_partial = self.vcf_repulsion_loss(
                    continuous_emb=encoded_embeddings,
                    sid_tokens=cluster_ids,
                    item_ids=item_ids,
                    exposure_times=exposure_times,
                    content_embeddings=input_embeddings,
                    positive_pair_matrix=positive_pair_matrix,
                )
```

**后果**：当 `use_vcf=True, use_vcr_td=False, use_tcl=False` 时：

```
condition = (False or False) and True = False
```

整个 VCF 块被跳过，**`vcf_repulsion_loss` 永远不会被调用**，等同于 G0 基线。

**这意味着你的消融实验 G6（VCF w/o TCCL，即 `use_vcf=true, use_tcl=false`）目前实际上跑的是 G0 基线**！

修复只需一行：

```python
# 第795行，改为：
condition = (self.use_vcr_td or self.use_tcl or self.use_vcf) and self.trainer.state.fn != TrainerFn.PREDICTING
```

---

### 🟠 Bug 2：`eval_step` 对 VCF 的排斥损失做了二次加权

```1257:1261:src/modules/clustering/residual_quantization.py
        loss = (
            self.quantization_loss_weight * quantization_loss
            + self.reconstruction_loss_weight * reconstruction_loss
            + self.lambda_rep * repulsion_loss
        )
```

而 VCF 的 `repulsion_loss` 从 `model_step` 返回时**已经在内部做过加权**（第498行）：

```497:499:src/modules/clustering/residual_quantization.py
        # 总损失
        L_repulsion = self.lambda_full * L_full + self.lambda_partial * L_partial
        
        return L_repulsion, n_full, n_partial
```

`eval_step` 再乘一次 `self.lambda_rep`，会使验证损失比训练损失（第939行 `repulsion_weight = 1.0`）系统性偏大，`val/loss` 曲线失真，Early Stopping 失效。

对比训练步的正确处理：

```938:941:src/modules/clustering/residual_quantization.py
        if self.use_vcf:
            repulsion_weight = 1.0  # VCF 内部已处理权重
        else:
            repulsion_weight = effective_lambda_rep
```

`eval_step` 缺少同样的判断。**修复**：

```python
# 在 eval_step 中改为：
repulsion_weight = 1.0 if self.use_vcf else self.lambda_rep
loss = (
    self.quantization_loss_weight * quantization_loss
    + self.reconstruction_loss_weight * reconstruction_loss
    + repulsion_weight * repulsion_loss
)
```

---

### 🟠 Bug 3：消融 G4（VCF w/o Dynamic Margin）的 `use_dynamic_margin` 标志对 VCF 不生效

```108:108:src/modules/clustering/residual_quantization.py
        self.use_dynamic_margin = kwargs.get("use_dynamic_margin", False)
```

这个 flag 只被 `vcr_td_repulsion_loss` 用到：

```550:553:src/modules/clustering/residual_quantization.py
        if self.use_dynamic_margin:
            margin = self.compute_dynamic_margin(content_embeddings, self.m0)
        else:
            margin = torch.full((batch_size, batch_size), self.m0, device=device)
```

而 `vcf_repulsion_loss`（第471行）**无论如何都会调用 `compute_dynamic_margin_vcf`**，`use_dynamic_margin=False` 对它没有任何效果。

**结果**：G4 消融组（VCF w/o Dynamic Margin）无法通过配置关闭动态边界，实际跑出来的就是完整 VCF，与 G1 结果相同，这个消融对比完全失效。

**修复**：在 `vcf_repulsion_loss` 中加入判断：

```python
# Stage 4: 动态边界计算
if self.use_dynamic_margin:
    margin = self.compute_dynamic_margin_vcf(
        content_embeddings, H, self.m0, self.hamming_radius, self.severity_beta
    )
else:
    # 退化为 QuaSID 静态边界
    margin = torch.zeros(B, B, device=device)
    margin[H == 0] = self.m_full   # 需要增加 self.m_full 参数，对应 QuaSID 的 m_full=0.8
    mask_partial = (H > 0) & (H <= self.hamming_radius)
    margin[mask_partial] = self.m_partial  # 对应 QuaSID 的 m_partial=0.5
```

---

### 🟡 发现的设计问题：TCCL 用的是 `quantized_embeddings` 而非 `encoded_embeddings`

```957:959:src/modules/clustering/residual_quantization.py
                l_cl = self.time_aware_collaborative_contrastive_loss(
                    quantized_embeddings, positive_pair_matrix=positive_pair_matrix, exposure_times=exposure_times)
```

而 VCF 的排斥损失用的是 `encoded_embeddings`（第862行）。两个组件作用在不同的表示空间：
- **排斥**：作用在编码器输出（量化前），直接优化编码器参数
- **TCCL**：作用在 codebook 向量求和（量化后），梯度通过 STE 传回

QuaSID 论文中对比学习直接用编码器输出 $\mathbf{z}_i$。建议统一为 `encoded_embeddings`，否则两个损失作用在不同空间，会互相干扰。

---

## 完整的修改优先级重排

综合 Kimi 核实 + 实际代码审查，修正后的优先级表：

| 优先级 | 问题 | 影响 | 状态 |
|--------|------|------|------|
| 🔴 P0 | **`condition` 缺 `use_vcf`，VCF 核心路径被跳过** | G6 等配置等于基线，实验数据全部失效 | ❌ 需修复 |
| 🔴 P0 | **`eval_step` 对 VCF 排斥损失二次加权** | val/loss 失真，Early Stopping 失效 | ❌ 需修复 |
| 🔴 P0 | **G4 消融（w/o Dynamic Margin）flag 对 VCF 不生效** | G4 与 G1 结果相同，消融失效 | ❌ 需修复 |
| 🔴 P0 | **消融缺少 QuaSID 静态基线组** | 无法证明 VCF > QuaSID | ❌ 需增加 |
| 🔴 P0 | **消融只评估 SID 训练损失，未跑下游 TIGER** | 结论无法支撑 | ❌ 需补充 |
| 🟠 P1 | `use_dynamic_margin=False` 需在 VCF 路径实现静态边界回退 | G4 消融才能跑通 | ❌ 需修复 |
| 🟠 P1 | TCCL 建议改用 `encoded_embeddings` | 与排斥损失作用空间统一 | ⚠️ 建议改 |
| 🟠 P1 | t_min vs t_max 变化在文档中需明确说明 | 论文前后一致性 | ⚠️ 文档改 |
| 🟡 P2 | 公式 4.2 与 Stage 6 实现不等价，文档需统一 | 可读性 | ⚠️ 文档改 |
| 🟡 P2 | 两份文档 G* 编号冲突 | 写论文时混乱 | ⚠️ 文档改 |
| ~~P0~~ | ~~R 未传参~~ | ~~已用 self.hamming_radius~~  | ✅ 误报 |
| ~~P2~~ | ~~min_margin/max_enhance 未实现~~ | ~~已在代码实现~~ | ✅ 误报 |
| ~~P2~~ | ~~Warmup 未实现~~ | ~~已在代码实现~~ | ✅ 误报 |

**最紧急的是 Bug 1（condition 缺 use_vcf）**——这会导致你目前所有用 `use_vcf=true, use_tcl=false` 配置跑出来的实验结果全部等同于基线，实验数据需要重跑。需要先修复这个 bug 再继续跑实验。