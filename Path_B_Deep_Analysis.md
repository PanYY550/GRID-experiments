# Path B 深度分析：为什么 α=0.01 最优？α=0.05 为何出现过拟合？

---

## 1. 非对称排斥的梯度机制

### 公式

```
α_i = clamp(exp_i / (exp_hot + exp_cold), α_min, 0.5)

z_i_mixed = α_i · z_i  +  (1 - α_i) · detach(z_i)
```

- **热门物品**：exp 高，α ≈ 0.5，获得 ~50% 梯度（承担排斥主力）
- **长尾物品**：exp 低，α ≈ α_min，仅获得 α_min 的梯度（受保护）

对每个碰撞对 (hot, cold)：
```
∂L/∂z_hot ≈ full gradient     ← 热门物品全量移动
∂L/∂z_cold ≈ α_min · gradient ← 长尾物品被"锁定"
```

### 三种配置的行为

| 配置 | 热门梯度 | 长尾梯度 | 长尾行为 |
|------|:------:|:------:|------|
| B0ctrl (对称) | 100% | **100%** | 热门拖拽长尾，长尾全力反抗 |
| B0 (α=0.05) | 150%* | **5%** | 长尾微微移动，但不足以逃脱 |
| B0a (α=0.01) | 150%* | **1%** | 长尾几乎冻结，热门绕行 |

*λ_scale=1.5 补偿：B0/B0a 的总排斥力通过 λ 放大 1.5x 以补偿梯度缩减

---

## 2. 为什么 α=0.05 是"最差中间地带"

### 2.1 实验数据

```
              VAL ndcg@10    TEST ndcg@10    VAL→TEST 下降   碰撞率
B0ctrl (对称)   0.03816        0.02690         -29.5%        1.21%
B0 (α=0.05)    0.03681        0.02765         -24.9%        2.12%
B0a (α=0.01)   0.04017        0.02945         -26.7%        2.56%
```

三个关键现象：
1. B0 的 VAL 最低（甚至低于对称基线）
2. B0 的 VAL→TEST 下降最小（过拟合程度最轻）
3. B0 的 TEST 高于 B0ctrl 但远低于 B0a

### 2.2 机制解释

α=0.05 创造了一个**"半推半就"的困境**：

```
对称 (α=1.0):  z_cold ─────────→ 移动到有效位置  (移动充分，语义保持)
α=0.05:       z_cold ──→         移动到半路       (移动不足，语义受损)
α=0.01:       z_cold ·           几乎不动         (语义保持最佳)
```

**5% 梯度的问题**：
- **太小**，无法将长尾物品推到有意义的低碰撞区域
  - 证据：B0 碰撞率 2.12% > B0ctrl 1.21%，说明排斥效果确实减弱
- **太大**，足以让长尾物品偏离其原始语义位置
  - 证据：B0 VAL 0.03681 < B0ctrl 0.03816，说明训练分布的表示质量反而下降

**1% 梯度的优势**：
- 长尾物品几乎不移动 → 语义保持最高保真度
- 热门物品承担全部排斥（λ_scale=1.5 保证排斥力不衰减）
- 热门物品"绕开"长尾，而非长尾被推开
- 结果：更干净的嵌入空间划分

### 2.3 类比：信号与噪声

```
长尾梯度 = 信号 + 噪声

α=1.0 (对称): 信号 > 噪声  →  有效移动（但热门拖拽效应损害 TEST）
α=0.05:       信号 ≈ 噪声  →  无效抖动（损害 VAL，TEST 略好于对称仅因热门拖拽减弱）
α=0.01:       信号 ≈ 0     →  完全不动（语义最高保真，TEST 最优）
```

## 3. 为什么 B0 的 VAL→TEST 下降最小（过拟合最轻）

### 3.1 VAL 过拟合的两个来源

1. **热门拖拽**：训练集中热门-长尾共现模式在 VAL 中重复出现，对称模型利用这些模式过度优化表示
2. **表示过度调谐**：encoder 输出为适配训练集的碰撞模式而调整，损失了泛化性

### 3.2 各组过拟合程度

| 机制 | B0ctrl | B0 | B0a |
|------|:-----:|:--:|:---:|
| 热门拖拽 | 强 | 中 | 弱 |
| 表示过度调谐 | 强 | 中 | 弱 |
| VAL→TEST 下降 | -29.5% | **-24.9%** | -26.7% |

B0 刚好处于"热门拖拽减弱了，但表示调谐还没完全消除"的中间状态——所以 VAL→TEST 下降最小。

但 **B0a 的绝对 TEST 最高**，因为更干净的语义保持带来的增益远超过拟合减少的收益。

### 3.3 关键洞察

> 降低过拟合 ≠ 提升 TEST 性能。
> B0 降低了过拟合（更小的 VAL→TEST gap），但以牺牲 VAL 表示为代价。
> B0a 同时做到了更好的 VAL 表示 + 更好的 TEST 泛化。

## 4. 数据分布视角：Beauty 数据集的长尾特性

Amazon Beauty 的曝光分布极度偏斜：

```
热门物品 (top 5%):   占据 ~80% 的曝光
长尾物品 (bottom 80%): 占据 ~5% 的曝光
```

这意味着几乎每个碰撞对都是 **1 热门 + 1 长尾**。

- **对称模式**：热门物品以全梯度"撞击"长尾 → 长尾被迫移动 → 长尾语义受损
- **α=0.01**：热门物品以 1.5x 全梯度移动自己来避开长尾 → 长尾纹丝不动 → 热门学会"尊重"长尾空间
- **α=0.05**：热门以 1.5x 梯度移动，但长尾也微微抖动 → 热门不确定该移多少 → 训练不稳定

## 5. 结论

### 5.1 α=0.01 最优的原因

1. **干净的角色分离**：热门移动，长尾冻结，各司其职
2. **最高语义保真度**：长尾物品不受热门梯度污染
3. **热门有效学习**：λ_scale=1.5 保证总排斥力不衰减，热门有足够信号学习绕行
4. **全或无原理**：在极度偏斜的数据分布下，"几乎不移动"(1%) 远优于 "移动一点点"(5%)

### 5.2 α=0.05 的过拟合本质

不是传统意义上的过拟合（训练远好于测试），而是**欠拟合 + 噪声的混合**：
- VAL 上：梯度不足导致有效碰撞减少不充分（欠拟合）
- TEST 上：部分保护减轻了热门拖拽（这点优于对称）
- 净效果：VAL 低于对称，TEST 略高于对称，远低于 α=0.01

### 5.3 实践建议

- **推荐配置**：α_min=0.01, λ_scale=1.5
- α_min 应在 [0.005, 0.02] 区间搜索最优值
- α_min > 0.05 可能退化到对称行为，不推荐
- 对于曝光分布更均匀的数据集，最优 α_min 可能更大
